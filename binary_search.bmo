binary_search(array, num):
	0 -> lo
	8 -> hi
	while lo <= hi:
		print "yo"
		lo + (hi - lo) / 2 -> mid
		print array[mid]
		if array[mid] == num:
			return mid
		if array[mid] > num:
			mid -> hi
		if array[mid] < num:
			mid -> lo
	-1

main:
	{1, 2, 3, 4, 5, 6, 7, 8, 9} -> array
	print binary_search(array, 7)
